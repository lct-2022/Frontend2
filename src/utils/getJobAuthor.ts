import { Application, Job, User } from "../types";

export const getJobAuthor = (applications: Application[], authUserId: number) => {
    jobs.forEach(job => {
        job.team.project.project.author_id
    });
}